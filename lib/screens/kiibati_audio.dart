import 'package:flutter/material.dart';

class KibatiiOnlineAudioScreen extends StatelessWidget {
  const KibatiiOnlineAudioScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Kiibati Audio is coming soon !!!"),
    );
  }
}
